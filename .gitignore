# Python
.venv/
__pycache__/
*.pyc

# OS
.DS_Store

# Claude Code local settings (personal permissions, not shared config)
.claude/settings.local.json

# Raw/archival EBL source data and the working database -- large, local-only
# working data. Races are added and reviewed locally (Claude Code + the
# pipeline scripts); only the *generated static site* (races/*.html,
# index.html, races.json) is versioned and shipped -- see build_race_db.py /
# render_*.py for how it's produced from these.
W2K_Dump/
ebl_data/
*.ebl
race_sessions.db
ebl_manifest.json
course_data.json
polar_data.json

# Old exploratory artifacts from before the web app existed
*.n2k.csv
*x.n2kdecoded.csv
out_008.csv
config_008.json
course_visualization.html
polar_visualization.html
w2k_dump_summary.*

# Third-party reference PDFs (Actisense manuals) -- not ours to redistribute
Docs/
.env

# Raw and overlaid race video (large, local-only) -- never shipped
Videos/
